import React from 'react';

export default class Articles extends React.Component {
  render() {
    return (
      <div id={ 'articles' } className={ 'view--full-height' }>
        articles
      </div>
    );
  }
}