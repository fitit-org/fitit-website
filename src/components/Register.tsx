import React from 'react';

export default class Register extends React.Component {
  render() {
    return (
      <div id={ 'register' } className={ 'view--full-height' }>
        register
      </div>
    );
  }
}