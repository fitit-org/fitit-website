import React from 'react';

export default class Login extends React.Component {
  render() {
    return (
      <div id={ 'login' } className={ 'view--full-height' }>
        login
      </div>
    );
  }
}